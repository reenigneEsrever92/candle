
fn conv1d() {}

fn matmul(

) {}

fn im2col_f32(
    gid: vec3<u32>,
    input: array<f32>, 
    output: array<f32>, 
    batch_size: u32,
    input_length: u32,
    channels_in: u32,
    channels_out: u32,
    kernel_size: u32,
    groups: u32,
    padding: u32,
    stride: u32,
    dilation: u32,
) {

}